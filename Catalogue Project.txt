Catalogue Project

Runtime configuration belongs in .env.local and must not be committed.

Required variables:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY

Security notes:
- Do not store service-role keys in this repository.
- Rotate any Supabase keys that were previously committed.
